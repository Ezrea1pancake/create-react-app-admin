import React from 'react';
import { Button } from 'antd';
import './App.less';
import { hot } from 'react-hot-loader/root';

const App = () => (
    <div className="App">
        <Button type="primary">Button111222333</Button>
    </div>
);

export default hot(App);
